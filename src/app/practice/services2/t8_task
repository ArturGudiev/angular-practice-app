1 provide one service originally
2 replace using more robust syntax
	{provide: LoggerService, useClass: LoggerService}

3 just replace useClass plainLoggerService
  both implement the same interface

4. do the same but with useExisting recipe

5. useValue --- it doesnt use new keyword to create a new instance of a class
it uses whatever value I provided

object literal with 2 methods
  log
  error


use factiry


export function dataServiceFactory(logger: LoggerService) {
  let dataService: DataService = new DataService(logger);

  // do more stuff to configure the service if necessary

  logger.log('Creating a new data service with a factory function.');

  return dataService;

}


{ provide: DataService, useFactory: dataServiceFactory, deps: [LoggerService] }


